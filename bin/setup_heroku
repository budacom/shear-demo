#!/bin/sh

# Setup heroku application remotes
if heroku auth:whoami &> /dev/null; then
  if heroku apps:info --app pl-guillotine-demo-staging &> /dev/null; then
    git remote add staging git@heroku.com:pl-guillotine-demo-staging.git || true
    git config heroku.remote staging
    echo 'You are a collaborator on the "pl-guillotine-demo-staging" Heroku app'
  else
    echo 'Ask for access to the "pl-guillotine-demo-staging" Heroku app'
  fi

  if heroku apps:info --app pl-guillotine-demo-production &> /dev/null; then
    git remote add production git@heroku.com:pl-guillotine-demo-production.git || true
    echo 'You are a collaborator on the "pl-guillotine-demo-production" Heroku app'
  else
    echo 'Ask for access to the "pl-guillotine-demo-production" Heroku app'
  fi
else
  echo 'You need to login to heroku. Run "heroku login"'
fi
